# The API origin the browser calls. Build-time: inlined into the client bundle.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Where Server Components send requests, so server traffic can stay internal.
# Falls back to NEXT_PUBLIC_API_URL when unset.
# API_URL=http://api:8000
